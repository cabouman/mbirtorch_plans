# LaTeX build files go to tex_output/ (see latexmkrc); the PDF builds here.
tex_output/
*.pdf
# The chart PDFs that make_figures.py writes are kept; they are the deck's figures.
!images/*.pdf

# The fusion image is committed: its source is on the cluster.
!images/ornl_fusion_zoom.png
